version: '3'
services:
  app:
    build:
      context: ../
      dockerfile: ./build/Dockerfile    
    environment:
      - SQL_DATABASE_URI=postgresql://postgres:bonaventura@host.docker.internal:5432/emlab_ai
      - DEBUG=True
    ports:
      - 8080:8080
